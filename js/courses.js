// zombie golf course
// v0.01
// sam thompson

var courses = {
    showAvailable : function () {
        if (state.roundsStarted === 0) {
            $('.greenerPastures').show();
        }
    },
    greenerPastures : {
        name : "Greener Pastures"
    }
};